#include <iostream>
#include <stdio.h>
#include <cstdlib>
using namespace std;
int main()
{
    int t;     // t be the number of test cases//
    cin>>t;
    if( 1<=t<=1000)
    {
     for( int m=0;m<t;m++)
    {
          int q;
          int a[3];
          for( q=0;q<3;q++)
          {
            cin>>a[q];
          }
          // x weight of mango y be the weight of truck z be the weight  of bridge// 
        
        if( (1<=a[0],a[1],a[2]<=100) && (a[0]+a[1]<=a[2])) 
        {
            int  k=(a[2]-a[1]);
              int  m=kdiva[0]
               if( m>0 || k>0 )
               {
                cout<<m<<endl;
               }
               else if ( k==0 || m==0 )
               {
                cout<<"0"<<endl;
               }
        }

    }

    
    }
}